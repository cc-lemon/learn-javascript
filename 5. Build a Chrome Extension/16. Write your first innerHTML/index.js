// Use .innerHTML to render a Buy! button inside the div container
const buttonEl = document.getElementById("container");


buttonEl.innerHTML += "<button>" + "BUY" + "</button>"; // alt "<button> + "BUY" + </button>"
